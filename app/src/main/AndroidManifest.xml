<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject">
        <activity android:name=".menu.SettingActivity"></activity>
        <activity android:name=".menu.AppInfoActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".note.EditNoteActivity" />
        <activity
            android:name=".note.AddNoteActivity"
            android:label="@string/title_activity_add_note"
            android:theme="@style/Theme.AndroidProject.NoActionBar" />
        <activity
            android:name=".note.NoteDetailActivity"
            android:label="@string/title_activity_note_detail"
            android:theme="@style/Theme.AndroidProject.NoActionBar" />
        <activity android:name=".login.ForgotPasswordActivity" />
        <activity android:name=".login.RegisterUserActivity" />
        <activity
            android:name=".note.NoteActivity"
            android:theme="@style/AppTheme.DarkActionBar" />
        <activity android:name=".login.MainActivity" />
    </application>

</manifest>